# 【已作废】MAINLINE §6 的第二张执行表（2026-08-22 从 MAINLINE.md 移出）

> 🚫 **这不是现状。** 保留仅为追溯（约束 2：结论被推翻时保留原文并标注理由，不静默删除）。

## 为什么作废

该表原本紧跟在 §6 正表之后，中间只隔一行残缺分隔符 `---|---|---|---|`，
极易被下一个线程误读为现状。其内容与台账 §5 **直接冲突**：

| 该表的说法 | 实际 |
|---|---|
| `~~P0-B~~ … 比值 0.541 ⇒ **算子级占 54%，图层面占 46%**` | 🔴 **已推翻**（#87/#95）：装置漏 `--config_file`，被静默编成 dspArch 68/vtcm 4MB。正确值 **0.036 ⇒ 图层面 ≈96%** |
| `**新 P1** #88 图层面 46%：#33/#34 融合与 VTCM 分块` | #88 已订正为 ≈96% 并由 **#97 解释**（RmsNorm 固有放大）；#33/#27 已关闭为「对量化图不适用」（官方 schema 注释 + 实测 md5 相同） |
| `3 #33 / #34 融合与 VTCM 分块 … 首选去处` | 同上，#33 已关闭；#34 部分澄清（`.bin` 路径下 `vtcm_mb` 对单算子无效果） |
| `7 #10 / #27 … 需换 snpe-dlc-graph-prepare` | 已执行并关闭（#93：根因是配置缺 `graphs` 段；graph-prepare 的设置**带不进 `.bin`**） |

⇒ 按这张表行动会**重跑至少三条已关闭的路**，正是约束 6 要防的最贵错误类型。

---

## 原文（逐字保留）

---|---|---|---|
| ~~P0~~ | ✅ **已完成（2026-08-21）·假设被证否**：反相关不是钳位假象，#69/#70 更强 ⇒ **P1 三层指标面板确认必要**。纯宿主，未占设备。见 §15.23 | — | — |
| ~~P0-A~~ | ✅ **已完成**：`fp32_final_latents_inloopref_vaefix.raw` + PNG（与两次在环运行**同噪声同 caption**，md5 逐位相同；修正后喂法；2304.6 s） | — | — |
| ~~P0-C~~ | ✅ **已完成**：我们 FP32 vs 官方 = **1.018 / 41.61 dB / 1.0016×** ⇒ FP32 参考可信 | — | 见 §15.22.10 |
| ~~P0-B 前置~~ | ✅ **已完成（2026-08-21）·通过**：两种 schema 都能注入 per-axis（#85）。⚠️ axis 会被重映射，必须读回验证 | — | — |
| ~~P0-B~~ | ✅ **已完成（2026-08-22）**：E_standalone 1.5544% / E_ingraph 2.8714% / 比值 0.541 ⇒ **算子级占 54%，图层面占 46%**。见 #87/#88、§15.25 | — | 抽真实 FC 的精确输入/权重/encoding，不重新校准，建单算子 context。**一次性二分**「真实数据/encoding 触发」vs「完整图才触发」——这是 #30（合成探针说 HTP 正确）与 #39（真实 FC 差 20~230 倍）矛盾的唯一分离刀 |
| **新 P1** | **#88 图层面 46%**：#33/#34 融合与 VTCM 分块 | 是 | P0-B 量化出来的那 46%。#30 已否定单算子普遍问题，但 #87 证明真实数据下算子级也有份 ⇒ 两条都在 |
| **P1** | teacher-forced + free-running 轨迹 | 是 | 拆开"单步局部误差"与"沿轨迹累积漂移"；记录 `Δx_{t+1}` 而非 raw `noise_pred`。dt 从 step0 −0.0714 到 step7 −0.3000（差 4.2 倍） |
| **P1** | 三层指标面板 | 部分 | 🔴 **已由 #84 确认必要**（不再是可选项）。决策门 = 多 seed 人工盲测 |
| **P2** | 4~8 seed 复现"马赛克 vs 清晰猫" | 是 | 排除 seed 偶然性（当前只有 1 个 A/B pair）。约 2.7 h 设备 |
| 3 | **#33 / #34 融合与 VTCM 分块** | 是 | **#30 已否定单算子问题 ⇒ 这里是首选去处** |
| 4 | **选择性 FP16**（#43 的正确形态） | — | 闸门已通（#45）。⚠️ 必须配 `--keep_weights_quantized`（#48） |
| 5 | **#29 offset 项精度** | — | 高优先级，#40 已给出机制证据 |
| 6 | **#26 向上游定位** | — | 沿 Id 0~306（余弦 >0.95 的可比区间）细分探测 |
| 7 | **#10 / #27** | — | 需换 `snpe-dlc-graph-prepare`（此前用错工具，被静默忽略） |
| 8 | **#32 SDK 版本 A/B** | — | 🔻 已降级。若做，用 #30 的极小探针做廉价 A/B |
| 9 | **#21 v79 成熟度** | — | 🚫 受阻（缺 v73 设备），**保留不删** |
| P3 | CLOSED ledger 按"机制性 / 旧 proxy 下"重分类 | 否 | #7 / #19 / #20 / #66 **不得再写"已证明无效"** |

> **零成本开关已全部用尽**：#9 / #19 / #25 / #42 / #44 全部证否或不适用；#16 / #17 / #18 因体积或速度出局。
> **剩下的每一条都要动 encoding 或动模型结构，都要经过 #45 这个闸门。**

