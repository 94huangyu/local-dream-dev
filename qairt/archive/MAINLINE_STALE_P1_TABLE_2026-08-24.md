# 【已作废】MAINLINE §6 的第二张执行表（2026-08-24 移出）

> 🚫 **这不是现状。** 保留仅为追溯（约束 2）。

## 为什么作废（三处与 §4 台账直接冲突）

| 该表的说法 | 实际 |
|---|---|
| `P1 #53 激活校准` | ❌ **已关闭**：代价门否决——mse/sqnr/entropy **根本不收紧量程**，`E_bulk` 4.4827/4.4571/4.4741% vs min-max 4.4741%，纹丝不动（§15.34）|
| `P1·新首位 选择性 FP16（#48）——最小确认先行` | ✅ **已做完**：单算子最小确认通路成立但收益比值 **0.981**（🟡）；真正有效的是**手术式 FP16**（#106，L3 +3.60 dB）|
| `P2 三层指标面板` | ✅ **已建成**（2026-08-23，§2.5），并已用它做出 4 个配置的判决 |
| `P1 把算子级归因搬到 part2` | ✅ 已做（#104），但**归因结论已被 #106 推翻**（探针太稀）|

⇒ 按这张表行动会重跑至少两条已关闭的路。**这是 2026-08-22 那次同类事故的重演**（当时移出的表见 `MAINLINE_STALE_P0_TABLE_2026-08-22.md`）。

🔴 **教训**：改写 §6 时必须 **grep 全文确认只有一张执行表**，不能只在开头插入新表。已连续两次栽在这里。

---

## 原文（逐字保留）

> 本表只列**活的**条目。已完成/已关闭的见台账 §5。

| 序 | 条目 | 设备 | 成本 / 前提 |
|---|---|---|---|
| **P1** | 🔴 **#53 激活校准**（min-max 的关闭理由已被 #52 动摇） | 是 | **重新量化约 3 h + context 约 3 h + 上机**。<br>⚠️ **必须先过 G0-cost**（输出域口径，见 §15.26 的教训）；percentile 那次是大幅倒退 |
| **P1·新首位** | 🔴🔴 **选择性 FP16（#48）——最小确认先行** | 是（短） | **体积否决已作废**（指南 §二十七：FC 支持 `FP16 激活 + SFxp8 权重`，权重字节不变）。<br>🔴 **不要直接上全模型 3h+3h**。先用 #30/#p0b 的**单算子探针**做最小确认，回答三问：① 量化器/建图**真的会产出** `FLOAT_16 激活 + SFxp8 权重` 吗（读回 DLC 与 context 验证，不看命令行）；② 该算子精度相对正确定点是升是降；③ FP16 的**速度**代价。<br>⚠️ 该配置要求权重 **SFxp8 对称 per-axis**，与当前 uFxp_8 不同；**不得引用 #7「对称更差」**（全定点设定下测的，迁移需先验证）。⚠️ #10/#27 的划界：图里出现真 fp16 段落时**需重测** |
| **P1** | 🔴 **把算子级归因搬到 part2** | 是 | §3.2：**part2 是支配项（41 of ~44 pp）**，而至今所有算子级工作都在 part1b 的 `linear_99` 上。<br>**这是「修最方便测的而非最该修的」，已自审记录** |
| **P2** | **三层指标面板** | 部分 | #84 已确认**必要**（不再可选）。决策门 = **多 seed 人工盲测** |
| **P2** | teacher-forced + free-running 轨迹 | 是 | 拆开「单步局部误差」与「沿轨迹累积漂移」；记录 `Δx_{t+1}` 而非 raw `noise_pred` |
| **P2** | 4~8 seed 复现「马赛克 vs 清晰猫」 | 是 | 现有结论只有 **1 个 A/B pair**。约 2.7 h 设备 |
| P3 | #56b 复查基于 `E_all` 的历史判断 | 否 | 与 #84 相关但独立（`E_all` 不经 VAE） |
| P3 | #38 FC 权重 per-tensor 正式确认 | 否 | 10 分钟，大概率只是复述 #44 |
| P3 | #86 自定义尺寸 / 非 1:1 | — | 可行（local-dream 用 zstd 增量分发每分辨率一份 context），约 1 天/分辨率 + 中等 app 改造。**不在关键路径** |
| P3 | CLOSED ledger 按「机制性 / 旧 proxy 下」重分类 | 否 | #7 / #19 / #20 / #66 **不得再写「已证明无效」** |
| ⛔ | #21 v79 成熟度 / #32 SDK 版本 A/B | — | 受阻（缺 v73 设备）／已降级。**保留不删** |

> **零成本开关已全部用尽**：#9 / #19 / #25 / #42 / #44 证否或不适用；#16 / #17 / #18 因体积或速度出局；
> #10 / #27 / #91 / #92 本轮关闭。**剩下每一条都要动 encoding 或动模型结构。**

> 🚫 **原先此处还有第二张执行表，已于 2026-08-22 移出** —— 它把 #87 的 `算子级占 54%/图层面 46%`
> 当成现状（该结论已被 #95 推翻，正确值 **≈96%**），并把已关闭的 #33/#10/#27 列为待办。
> 逐字原文与作废理由见 `archive/MAINLINE_STALE_P0_TABLE_2026-08-22.md`。**上表才是现状。**

---


