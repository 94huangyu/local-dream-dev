# Z-Image Turbo on HTP：画质缺陷定位到 VAE 导出 —— 排查结果与后续计划（供外部审查）

生成时间：**2026-08-19**
标注约定：**①实测数据** ／ **②由数据严格推出的结论** ／ **③未验证的假设**
目的：**请审查逻辑链** —— 哪条归因跳步、哪个前提没验证、后续计划的优先级是否正确。

---

## 〇、三句话

1. 用户从一开始指出的画质缺陷（**毛发蜡质、边缘发硬**），经单变量实测
   **100% 来自我们的 VAE ONNX 导出**，在 FP32、零量化下即可复现。
2. ⇒ **它与 HTP、与量化、与 transformer 全都无关。** 此前针对该缺陷的量化优化，靶子是错的。
3. VAE 的**图结构与权重与官方完全等价**（逐项核对），⇒ 缺陷是**运行时数值行为**，
   具体成因**尚未定位**（③ 最可疑：GroupNorm→InstanceNormalization 分解的方差精度）。

---

## 一、上一份审查（`ZImage_HTP_Project_Route_Review_2026-08-18.md`）的处理

审查提出"阶段 A（HTP correctness）/ 阶段 B（quality recovery）+ 三个 Gate"的框架，
我**采纳**，并指出它缺一个前置阶段：**阶段 0 —— 导出保真度**。
理由：审查默认 FP32 参考是有效靶子，而我们的 FP32 参考跑在自己的 ONNX 导出上，
**该导出从未与官方对照过**。若导出有缺陷，Gate B 会在产品仍明显劣于真实模型时通过。

**本轮就是在做阶段 0，而且它确实抓到了东西。**

审查其余采纳项：Exact-real-FC standalone replay（取代我原来的合成离群输入方案）、
8-step teacher-forced/free-running 轨迹、三层指标、固定 benchmark、
历史关闭项分"机制性 / 旧 proxy 下"两类。
保留意见一处：分层指标解决"诊断"但没解决"决策"，建议决策门定为**多 seed 人工盲测**。
成本修正一处：审查建议的 16 prompt × 4 seed，按实测 FP32 单张 2105 s ⇒ **37 小时宿主**，
建议先做 4 prompt × 2 seed。

---

## 二、阶段 0 的完整结果（①实测）

### 2.1 文本编码器导出：**忠实** ✅

| 检查项 | 官方 | 我们的 ONNX | 结果 |
|---|---|---|---|
| 取哪层隐藏态 | `hidden_states[-2]` | ONNX 含 35 层 / config 36 层 | ✅ 正是倒数第二层 |
| 数值 | Qwen3Model | 前 20 槽相对 L2 **0.8628%**、余弦 **0.999990** | ✅（差异在 bf16 舍入量级） |
| chat 模板 | `add_generation_prompt=True`，`enable_thinking=True` 不插 `<think>` | 硬编码串逐字相同 | ✅ |
| `Qwen3Model` vs `Qwen3ForCausalLM` | — | hidden_states 相对 L2 **0.0000%** | ✅ |

### 2.2 序列长度：**真差异，但已实测降级** 🔻

我们的 text_encoder 被**硬编码固定在 20 token**（ONNX 内部 Reshape 常量 `{1,20,-1,128}`；
输入 dim 虽声明为动态，但图内常量把它钉死），官方默认 `max_sequence_length=512`。
本项目 prompt 完整模板 **22 token** ⇒ 末尾 `<|im_start|>assistant\n` 被截掉。

**单变量实测**（22 vs 20，同噪声、同 transformer、同 VAE）：
像素差 20.27，**两张图同级，蜡质缺陷原样存在**。
⇒ ②**截断不是当前画质缺陷的主因。**

⚠️ **划界**：该结论**只对这个 22-token 短 prompt 成立**（只丢 2 个 token，是损失最小的情形）。
模板本身占 6 token ⇒ 正文只剩约 14 token 空间，**长 prompt 的代价未测**。
transformer 的 caption 上限是 **32 槽** ⇒ **只把 text_encoder 重导到 32 即可吃满现有容量，
且 transformer 不需要动**。这是产品能力问题，不在当前关键路径。

### 2.3 transformer 导出：**忠实** ✅

官方 PyTorch 全流程 vs 我们的 ONNX FP32 全流程（同噪声、同 caption、同调度器）：

| step | 官方 noise_std | 我们 ONNX | 相对差 |
|---|---|---|---|
| 0 | 1.5485 | 1.5522 | −0.238% |
| 3 | 1.3317 | 1.3318 | −0.008% |
| 7 | 1.2858 | 1.2836 | +0.171% |

⇒ ②**逐步差异 ±0.24%，8 步累积对成图贡献可忽略**（见 2.4 的双臂对照）。

### 2.4 🔴 VAE 导出：**缺陷所在**

**双臂对照**：同一份 final latents，唯一变量 = VAE 实现。

| | 官方全流程 vs 我们 ONNX 全流程 | **仅 VAE 双臂** |
|---|---|---|
| 平均像素差 | 13.627 | **13.326** |
| PSNR | 23.38 dB | **23.51 dB** |
| 高频能量（平均梯度幅值）差 | **+30.7%** | **+30.5%** |

⇒ ②**两组数字几乎重合 ⇒ 全流程的画质差异【全部】来自 VAE 这一步。**

**高频能量高出 30% 不是"更清晰"**，而是细节被推成高对比硬边 —— 与用户描述的
"蜡质、发硬"以及我亲自看图的判断一致（官方毛发柔和有层次，我们的成缕发硬）。

### 2.5 VAE 静态检查：图与权重**完全等价** ⇒ 缺陷是运行时数值

| 检查项 | 官方 decoder | 我们的 ONNX | 结果 |
|---|---|---|---|
| Conv2d | 35 | Conv 35 | ✅ |
| GroupNorm | 30 | InstanceNormalization 30 | ✅ 标准分解 `Reshape→InstanceNorm→Reshape→Mul→Add` |
| SiLU 调用 | 14 block×2 + 1 = 29 | Sigmoid 29 | ✅ |
| Upsample2D | 3 | Resize `nearest`/`asymmetric`/`floor` | ✅ |
| Attention | 1 | Softmax 1 + MatMul 6 | ✅ |
| GroupNorm eps | 1e-6 | 1e-6 | ✅ |
| 权重 | — | 73 个实权重逐位吻合；4 个注意力权重**转置后**相对差 **0.000000** | ✅ |

⇒ ②**结构与权重完全等价，两臂均为 fp32 ⇒ 30.5% 的高频差异只能来自运行时数值行为。**

**③ 最可疑（未验证）**：GroupNorm 被分解为 `InstanceNormalization` 后，
在 1024×1024 分辨率下每组的空间元素数极大；若 onnxruntime 的 InstanceNormalization
用 `E[x²]−E[x]²` 的单遍公式计算方差，会产生显著数值损失，
表现为**对比度/锐度的系统性偏移**。

---

## 三、这对项目意味着什么

### 3.1 一个此前完全错位的归因

项目此前把"蜡质毛发"当作 HTP 量化问题追了数天。现在证明它**在 FP32 就存在**。
⇒ 即使 HTP 做到与 FP32 完全一致，**这个缺陷依然在**。

### 3.2 HTP 侧的成果不受影响

设备端已能生成清晰的猫（四段全 per-row），这条结论独立成立。
HTP 与 FP32 之间**仍有差距**（另一个问题），但**不是本缺陷的成因**。

### 3.3 方法论：又一次"数值对、图不对"

若只看逐步数值（±0.24%），会判定"导出忠实"并继续在量化上打转。
**是"打开图看"抓住的**。这与本项目已记录的 #69/#70（标量指标在当前区间不可靠）同源，
且这次是**正向印证**：数值几乎相同而画质差异明显。

---

## 四、后续计划（请重点审查优先级）

### P0：定位 VAE 数值发散点 —— 纯宿主，约 20~30 min

逐层对比官方 PyTorch VAE 与我们 ONNX VAE 的中间激活，找出**高频能量从哪一层开始发散**。
做法：PyTorch 侧用 forward hook 抓每个 block 输出；ONNX 侧把对应张量加为 graph output。

**判据（事前）**：
- 发散起点在某个 `InstanceNormalization` 之后 ⇒ ③ 假设成立，转 P1-A
- 发散起点在 `Resize`/`Conv` ⇒ 转 P1-B
- 逐层都小、末端才大 ⇒ 是累积效应，需重新设计

### P1-A：若确认是 GroupNorm 分解

候选修复（按代价排序）：
1. 用 **`GroupNormalization` 原生算子**重新导出（ONNX opset 18+ 有该算子），避免分解
2. 导出时把该处**强制 fp32 累加**或改用数值稳定的两遍方差
3. 若 ORT 实现有缺陷，改用其他 EP 或自定义算子

⚠️ **该缺陷是否会传导到设备侧的 QNN VAE，尚未验证** —— QNN 有自己的 GroupNorm 实现，
可能不受 ORT 的问题影响。**必须单独验证，不得外推。**

### P1-B：若是 Resize/Conv

查导出时的 opset 版本与算子实现差异。

### P2：回到 HTP 主线（上一份审查的阶段 A）

VAE 缺陷修复后，重新建立同噪声 FP32 基准，再做：
Exact-real-FC standalone replay → 8-step teacher-forced/free-running 轨迹 → 三层指标面板。

### P3（产品能力，不在关键路径）

text_encoder 重导到 32 token（吃满 transformer 现有容量，transformer 不动）。

---

## 五、请审查者重点检查的四处

1. **§2.4 的"全部差异来自 VAE"是否声称过强？**
   依据是两组数字（13.627/+30.7% vs 13.326/+30.5%）几乎重合。
   但两次对照用的 latents 不同源（官方那次的 final latents 未保存，
   VAE 双臂用的是我们 ONNX 那次的，末步 std 差 0.17%）。**这个替代是否影响结论？**

2. **"高频能量（平均梯度幅值）"这个指标本身可靠吗？**
   本项目刚刚栽在"用未验证的标尺下结论"上（#69）。
   该指标**未经已知样本验证**，我用它作为主要证据是否重蹈覆辙？
   （辅助证据：我亲自看图的判断与它一致，且两次对照数值高度一致。）

3. **③ 那个 InstanceNormalization 假设是否有更简单的替代解释？**
   例如 ORT 的 Conv 算法选择（Winograd vs 直接卷积）也可能改变高频特性。

4. **优先级：先修 VAE，还是先回 HTP 主线？**
   我把修 VAE 排在最前，理由是它在 FP32 就存在、且修复不依赖设备。
   但也可论证：设备侧最终用的是**量化 VAE**，ORT 的问题未必传导到 QNN，
   ⇒ 可能修了 ONNX 侧对最终产品无收益。**这条我没有验证，是当前计划最大的不确定。**

---

## 六、关键产物索引

- 成图：`scratch_runs/official_pytorch_22tok.png`（官方地面真值）、`fp32_22tok.png`（我们 ONNX）、
  `vae_ab_pytorch.png` / `vae_ab_onnx.png`（VAE 双臂）、
  `zimage_fp32_cxxlatents.png`（20-token 版）、`app_4seg_cat_seed42.png`（设备端）
- 噪声：`p0_experiments/latents_cxx_seed42.raw`（设备 C++ RNG 生成）
- latents：`p0_experiments/fp32_steps/lat_0..8.raw`（22-token 那次逐步）
- 脚本：`scripts/official_pipeline_run.py`、`vae_ab.py`、`official_caption.py`、
  `fp32_step_runner.py`、`cxx_latents.py`
- 环境：`D:\ZImage_Work\venv-official`（Python 3.12 + torch 2.13 CPU + transformers 5.15 + diffusers 0.39，
  **与用户的 ComfyUI 环境隔离**）

---

## 七、【2026-08-19 补】§5 第 4 条的不确定性已用实测关闭

原疑问：*"设备上跑的是量化 QNN VAE，QNN 有自己的 GroupNorm 实现，ORT 的问题未必传导
⇒ 可能修了 ONNX 侧对最终产品无收益。这条我没有验证，是当前计划最大的不确定。"*

**实测（同一份 final latents，三臂）**：

| VAE 实现 | 高频能量 | vs 官方 | 像素差 vs 官方 |
|---|---|---|---|
| 官方 PyTorch（fp32） | 2.892 | — | 0 |
| 我们的 ONNX（fp32/ORT） | 3.773 | **+30.5%** | 13.33 |
| **设备 QNN（量化/HTP）** | **4.021** | **+39.0%** | 13.28 |

⇒ ②**缺陷传导了，且被量化放大**（30.5% → 39.0%）。
⇒ ②**"先修 VAE 导出"这个优先级成立**，不再是未验证的赌注。
⇒ 结构清晰：**导出 +30.5%，量化 +8.5 pp**，两者可分别处理，导出是大头。

⚠️ 划界：量化那 8.5 pp 用的是当前 A16W8 per-tensor 配置；
**VAE 从未做过 per-row 等尝试**（它一直不是嫌疑对象），可优化空间未知。

⚠️ §5 第 2 条（"高频能量"这把尺子未经已知样本验证）**仍然成立，尚未解决**。
目前的辅助证据是：三次独立对照数值高度一致（+30.7/+30.5/+39.0），
且与我亲自看图的判断方向一致。**但这不等于验证过，请审查者仍按未验证对待。**

---

## 八、【2026-08-19 补】P0 逐层定位：**首次尝试的匹配方式无效，结果作废**

`scripts/vae_layerwise.py` 按「ONNX 的 `node_group_norm*` 输出」与
「PyTorch decoder 的 30 个 GroupNorm 模块」**按执行序 1:1 匹配**。

**该假设不成立**：第 2 个即出现形状不符 —— PyTorch `(1,512,128,128)` vs ONNX `(1,512,16384)`。
ONNX 侧 GroupNorm 分解后的中间形态是**展平的**，且节点命名顺序与模块调用序不一致。

⇒ 🔴 **该脚本输出的前两行（相对 L2 103.1010% / 136.1454%，余弦 0.4299 / 0.0650）
是错位比较的产物，不是发散证据，一律作废，不得引用。**

（若不看形状直接采信，会得出"第一层就已完全发散"的错误结论，
并据此否定"导出等价"这一已由权重逐位比对确立的事实。）

### 下次做这件事的正确做法（建议）

1. **不要按序匹配**。改用**形状 + 拓扑位置**双重锚定，或直接在 ONNX 侧
   取每个 `Add node_group_norm*` 之后、下一个 `Conv` 之前的张量，并断言其为 4 维。
2. **每一步都断言形状相等**（本次正是靠这条断言才发现错位，应保留并前置）。
3. 分辨率高的层（256²/512²/1024²）单个张量达 134 MB~1 GB，**必须分批抓**。
4. 更稳妥的替代：不做逐层，而是**二分**——先只比 mid_block 输出与最终 conv_out，
   缩小区间后再细分。
