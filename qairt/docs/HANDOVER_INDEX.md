# 交接文档导读 —— 先读这一页，再决定读哪几节

`HANDOVER_2026-08-13_ZIMAGE_MVP.md` 有 **约 5170 行 / 46 节**，是一个多月逐日累积的。
🔴 **它不是给人从头读的。** 其中 **1344 行（27%）是一条已被否定的方向及其推翻过程**，
另有约 500 行是第一天的临时操作笔记。直接顺读会被枝节淹没，还会误信已被推翻的结论。

本页把 46 节分成四类。**新接手者只需读「必读」的 11 节，约 1540 行。**

---

## 〇、30 秒版：项目在做什么、现在到哪了

**目标**：把 Z-Image Turbo（8 步蒸馏文生图）跑在骁龙 8 Elite（SM8750）的 HTP 上，做成可用的手机 app。

**两条并行主线**（见 `CLAUDE.md`）：
- **A 画质**：量化后画质与 FP32 的差距。当前最优 **18.67 dB**（起点 11.93，参照上限存疑见 #155）。
- **B 方法论**：把可复用的 QNN 转换/量化流程沉淀进 `docs/QNN_CONVERSION_GUIDE.md`（后续要转 Flux.2 Klein 等）。

**当前状态（2026-09-17，用户排定优先级 P1 → P2 → P3）**：
- ✅ 真机可用，五个比例（1:1 / 4:3 / 3:4 / 16:9 / 9:16）全部出图
- ✅ **P1 五比例模型过大 —— 已完成**：mg（多图共享权重）+ 共享 spill-fill 已交付并清理，
  设备模型 **35 → 12.31 GiB**，1:1 出图与原现网**逐字节相同**，换比例不再有校验等待（§15.45，#168 关闭）。
  残余：#170 单秒内存谷值成因未查、#171 **清理后无快速回滚**（staging 脚本未写）、#172 非 1:1 数值等价未验
- ✅ **P2 生图速度 —— 全部收口（2026-09-20）：用户实际等待 约 350 s → 约 148 s（−58%），出图逐字节不变，用户已验证交付**（§15.50，`scripts/EXP_PLAN_P2_SPEED.md`）。
  ①**契约校验**：软件 SHA-256 → ARMv8 硬件加速（129→19.4 s，#178）→ 结果缓存（19.4 s→**0 ms**，#182）；
  ①**装载改 mmap**：−17.5 s 且 MemAvail 余量 **+616 MiB**（#174）；①**删 part1a 的 ScatterElements**：−12.6 s（#173）；①UI 收尾（#180）。
  ①已查明**无空间**：HVX、VTCM、功耗档、宿主侧重量化+拷贝（占段墙钟 1~5%）、并行装载（官方文档非线程安全 #179）、
  **context 装载 12.7 s 已贴 I/O 极限**（1015 MB/s）。
  ⊕ 模型包 `/sdcard/Download/ZIMAGE_sm8750_v2.zip`（宿主副本 `D:\ZImage_Work\package\`），交付态快照见 `docs/DELIVERY_FROZEN.md`。
  🔶 残余：#170 内存谷值成因、#171 single 回滚脚本、#172 非 1:1 数值等价。
- **P3 与官方一致性** —— 根因已定位（§15.44），按用户指示**挂起**

**真正的入口是 `MAINLINE.md`**（台账 + 执行顺序），本文档是它的证据库。

### 给新线程的指令（2026-09-17 写，可直接粘贴）

> 1. 读 `CLAUDE.md`（约束）→ `MAINLINE.md` §2.1 与 §4 未关闭台账 → 本页 → `HANDOVER_2026-08-13_ZIMAGE_MVP.md` **§15.50**（P2 收口 + 本线程复盘，必读）与 **§15.45**（设备现态）。不要顺读全文。
>    ⊕ **要换模型（Flux.2 Klein 等）的话，入口是 `docs/QNN_CONVERSION_GUIDE.md` §47（新模型路线图），不是本页。**
> 2. 当前主线 = **P2 生图速度**。零设备验证已做完（§15.46，证据可用 `python scripts/p2_zero_device_evidence.py` 复算）。
>    **P2 已收官（用户已验证交付）**。下一步由用户定：跑通新模型后再做专用 app。
>    🔴 **要换模型（Flux.2 Klein 等）→ `docs/QNN_CONVERSION_GUIDE.md` §47**，且**先读 §47.10「五种反复出现的思维模式」**（比坑清单更值得先读：换模型后新坑不同，那五种模式不变）。
>    入口 `python scripts/p2_s6_session.py`（不带参数 = 纯宿主自检，`--run` 才碰设备，判据事前锁定、任一门不过自动回滚；
>    干跑台 `python scripts/p2_s6_dryrun.py all` 九场景先全过再上机）。
>    🔴 **设备现态是交付形态**：`LOAD_MMAP` marker **在设备上就是 H2 的交付物，不要删**；part1a 是 noscatA（2,877,374,464 B）；APK 的 `.so` 指纹 `538c36c58263b074`。完整快照见 **`docs/DELIVERY_FROZEN.md`**（由 `scripts/freeze_delivery.py` 现读生成）。
>    已关闭勿重试：O3（#147）、图切换（§43）、步数（#153）、VTCM 调大（§15.46.5：上限即 8）。
> ⊕ 2026-09-17 旧版第 2 条（「先解析 mg 的 `[segtime]`、查 part1a 为何 1.75 倍」）已执行完毕，结果见 §15.46。
> 3. 设备现态：mg 契约 + 新 APK + `SHARE_SPILLFILL=331415552`，**清理后无快速回滚**（#171）。手机是用户的：要插线必须明说「请插线」、多久、能否中途拔，用完明说可以拔。
> 4. 可复用结论必须同步进 `docs/QNN_CONVERSION_GUIDE.md`（主线 B）。汇报只写过去式；宿主 23.7 GB 内存，重活不并行，启动前先测余量。

---

## 一、必读（11 节，约 1540 行）—— 读完就能接手

| 节 | 行数 | 为什么必读 |
|---|---|---|
| **§15.50**【2026-09-20】P2 全部收口 + **本线程复盘** | ~55 | **最新·先读这节**。交付最终态与总账（约 350 s → 约 148 s）、剩余未关闭、**五种反复出现的思维模式**（已固化为指南 §47.10）、两个沟通/操作类错误 |
| **§15.49**【2026-09-19 夜~20】第三个杠杆 + UI 收尾 + 冻结 | ~50 | SHA-256 硬件加速为何最迟才发现、「先编独立可执行文件验证再集成」的做法、判据失效两次的同一个坑（埋点位置 vs 采集窗口） |
| **§15.48**【2026-09-19 20:17–20:57】两个杠杆交付 | ~45 | 归因表（每步只差一个变量）、mmap 的内存意外收益、三处**判据自身**的错误（叠加归因 / 终态判据 / 生效证据） |
| **§15.47**【2026-09-19】D2 收尾：A 变体定稿 + H2/H5 埋点 + S6 方案 | ~60 | 单删实验的最终结论（同型结构两个实例结论相反）、mmap/拆时两处埋点、S6 一次插线方案、宿主自检当场拦下的两件事 |
| **§15.46**【2026-09-17】P2 起步：零设备诊断 + 磁盘清理 | ~110 | **P2 的起点**。时间预算、part1a 单位算量慢约 1.5 倍的证据链、ScatterElements 嫌疑与编码核对、装载路径订正 #162、VTCM/功耗档/性能估算已无空间、一条被自己推翻的推论、**D1 接手清单（15.46.8）** |
| **§15.45**【2026-09-07~17】🟢 P1 交付：mg + 共享 spill-fill | ~110 | 设备现态、五次运行为何四次回滚、11 条教训、**未完成清单（15.45.6）= 新线程的起点** |
| **§15.44**【2026-09-06~07】🔴 主线 A 根因：Tier 2 手术引入 15.5% | 88 | P3（挂起）的全部结论。基座导出忠实（对官方 1.79%）；手术后 15.52%；机制 = padding **个数**；两个被自己判据推翻的假设（免得重试）；**面板为何看不见这类缺陷** |
| **§15.43**【2026-09-06】多比例交付完成 | 88 | single 形态的五比例数据（mg 交付前的状态）。五比例实测数据、mg 为何被否决、7 条踩坑、8 个新工具 |
| **§15.41** 三层指标面板 + 最优配置 | 56 | **怎么衡量画质**。L1/L2/L3 三层指标，报结论必须给 L3 |
| **§15.40** 四段手术式 FP16 端到端 | 53 | **当前部署的量化配置**从哪来（`E_all` 43.41% → 30.68%） |
| **§15.39** 手术式 FP16 有效 | 53 | 上一条的机制依据（主体余弦 0.7099 → 0.9455） |
| **§15.22** VAE 反缩放被做了两遍 | 177 | **画质史上最大的一次修复**，且是「文档描述当事实」的典型教训 |
| **§15.19** 设备出猫 + 四段交付上线 | 162 | **四段布局为什么是四段**（part2 拆分与 PD 红线） |
| **§15.17** 逐行量化 per-row | 111 | 迄今唯一有效的 HTP 修复手段之一 |
| **§十二** SM8750 芯片型号 + backend_extensions 正确格式 | 286 | **建图配置的唯一正确写法**。漏 `--config_file` 会静默编成 v68/4MB（§15.29 就栽在这） |
| **§十五** 根因：HTP 跑 `.bin` ≠ CPU 参考跑 `.dlc` | 353 | **整条量化主线的根因**。也是措辞纪律的来源：离线 `snpe-net-run` 的结论只能写「在 SNPE CPU 参考实现上」，**不得写「设备实测」** |

---

## 二、按需查（closed 线索的证据，别顺读）

只在「想知道某条路为什么被排除」时回查。**先查 `docs/LEDGER_CLOSED.md` 的一行索引，再决定要不要展开。**

| 节 | 主题 | 结论 |
|---|---|---|
| §15.14 | 三个量化开关 | 实测结论表 |
| §15.15 | `--quantization_overrides` 打通 | 🟢 闸门 |
| §15.18 | part2 拆分：从卡死到打通 | 🟢 |
| §15.20 | app vs PC 在环对照 | C++ 第 3 个 bug 假设关闭 |
| §15.21 | 画质缺陷定位到 VAE 导出 | 被 §15.22 精化（不是导出缺陷，是反缩放两遍） |
| §15.23 | #84 反相关不是钳位假象 | 假设被证否 |
| §15.24 | per-axis encoding 可注入 | 🟢 前置门 |
| §15.27 | 分块量化 BQ / per-row bias | 两条均关闭 |
| §15.28 | 「静默忽略」根因：配置缺 `graphs` 段 | 🔴🔴 重要机制 |
| §15.30 | 图层面误差 = RmsNorm 固有放大 | 非 HTP 缺陷 |
| §15.31 | RmsNorm gamma 8-bit | 次要（3.75%），划出「宿主消融」天花板 |
| §15.33 | #48 wFxp_actFP | 通路成立，收益不显著 |
| §15.34 | #53 激活校准 | 代价门否决 |
| §15.35 | #102 选择性 FP16 | `Pow(x,2)` 在 FP16 溢出 |
| §15.36 | #103 BF16 | 工具设计封死，「选择性浮点」整族关闭 |
| §15.32 | 查文档推翻「选择性 FP16 不可行」的前提 | 🟢 **查文档 2 分钟省数小时的样板** |

---

## 三、🔴 **勿据此推论**（结论已被推翻，原文按约束 2 保留）

**读到这些节时必须同时读它的推翻节**，否则会拿错误结论往下推。

| 节 | 行数 | 被谁推翻 | 错在哪 |
|---|---|---|---|
| **§十三** | **689** | §十四 | 整条「量化 override」方向被否定；transformer 量化不是根因。**本文档最长的一节，内容全是死路** |
| §15.16 | 100 | 自身 | 选择性对称权重：已执行、被否定，**且方向相反** |
| **§15.25 / §15.26** | 69+48 | **§15.29** | 建 context 漏传 `--config_file` ⇒ 编成 **v68 / 4 MB VTCM**（尽管传了 `--htp_socs`、文件名带 `.SM8750`）。正确装置下 `E_standalone` 是 **0.1034%** 而非 1.5544%，**差 15 倍**。数值一律不得引用 |
| §15.37 / §15.38 | 67+70 | §15.39 | part2 算子级归因「RmsNorm 主导」「收口」均被推翻 |
| §15.42 | 56 | 自身 | dtype 机制：工具有效，但**我的选择规则被推翻两次** |

⊕ §十四 / §十四·续（655 行）是推翻过程本身，**结论已并入 §15 及之后**，不必单读。

---

## 四、已过期（描述的是不存在的状态；保留仅为溯源）

§一、§二、§三、§四、§五、§六、§七、§八、§九、§十、§十一（约 500 行，2026-08-13～14）是项目头两天的逐时笔记：
「今晚做的三类修复」「当前正在诊断：生成的图像是错的」「给 MacBook 继续诊断的建议」等。
其中提到的问题**都已解决**（图像错误 → §15.22 的 VAE 反缩放；分词器 bug → 已修）。
🔴 **不要读，也不要引用其中的「当前状态」。**

例外：§十（128 行）里建立的**单变量测试纪律**仍然有效，但已升格进 `CLAUDE.md` 的约束，读约束即可。

---

## 五、真正该先读的其他文件（按顺序）

1. **`CLAUDE.md`** —— 11 条项目约束。**不是形式主义**，每一条都对应一次真实事故
2. **`MAINLINE.md`** —— 线索台账（未关闭条目全文）+ 执行顺序。**每轮开工必读**
3. **`docs/LEDGER_CLOSED.md`** —— 已关闭条目的完整证据。🔴 打算重试某条路、或新假设与某条已关闭结论相关时**必须回查**
4. **`docs/QNN_CONVERSION_GUIDE.md`** —— 主线 B 的独立交付物（48 章）。🔴 **换模型上手只读 §四十七 + §四十八**，顺序：
   **§47.10 五种思维模式 → §47.1 阶段表 → §47.1.1 六张执行卡片（命令+判据，照着跑）→ §47.2 坑清单（已按卡片索引）→ §四十八 接进 app**。
   ⊕ §四十七走到卡 E 只是"设备上数值正确"；**要在手机上真的跑起来看 §四十八**（挂载点清单、契约同步、SELinux、存档指纹）。
   ⚠️ **2026-09-20 做过一次一致性订正**：早期章节有 6 处被后来实测推翻却写成祈使句的写法
   （per-channel 空转、PSNR 当画质判据、`--use_native_*`、分段按进程内存、§一 验证状态、§九 overrides），
   已就地标注，**汇总表在文档顶部**。照抄早期命令前先看那张表。
   其余：共享 spill-fill §38.3、内存门的尺子 §37.x、Windows→Android 交付坑 §42.5、图切换 §43、padding 个数陷阱 §44、速度诊断 §45、**图级手术的硬规则 §47.3**
5. **`scripts/EXP_PLAN_P2_SPEED.md`** —— **P2 当前方案**（D1 设备诊断判据已锁定；D2 待补写）。证据复算 `scripts/p2_zero_device_evidence.py`
6. **`scripts/EXP_PLAN_SPILLFILL.md`** —— P1 的方案与五次运行逐条执行记录（含两次判据修订的理由）
7. **`scripts/oneshot_mg_session.py`** + **`scripts/oneshot_dryrun.py`** —— 设备会话脚本与干跑台（11 场景）；改前者必须跑后者，**但干跑查不出外部程序参数类问题**（§15.45.3 #6）
8. **`docs/RUNBOOK_ASPECT_DEVICE.md`** / **`docs/RUNBOOK_MG_SPILLFILL.md`** —— 插线手册（后者文首标注了与实际执行的差异）
9. ~~`scripts/EXP_PLAN_MEMSPEED.md` —— 下一步任务的方案~~（其 E/F 已否决、D 未做；P1 已由 spill-fill 路线完成，**不再是下一步**）
10. **`docs/EXECUTION_MODEL.md`** —— 只收录有实测证据的执行契约规则

---

## 六、维护规则

- 本索引与交接文档**同步更新**：新增一节就在这里归类，否则索引失效比没有索引更糟。
- 结论被推翻时，**在被推翻节的标题下加导航标注**（写清被谁推翻、错在哪、数值能不能用），
  原文一字不删（约束 2）。本索引第三类就是这么维护的。
- 判断「该归哪类」的标准：**新接手者不读它会不会做错事**。会 ⇒ 必读；
  只是少知道一个背景 ⇒ 按需查；读了会被误导 ⇒ 勿据此推论。
