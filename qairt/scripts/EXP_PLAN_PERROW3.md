# EXP_PLAN_PERROW3 —— 三段全部 per-row，设备能否生成正确的图？

定稿时间：2026-08-16 09:35（**执行前定稿，判据与决策树不得事后修改**）
台账归属：**#54**（由 #47 导出）
前置：`EXP_PLAN_PERROW`（part1b 单段：cos_bulk 0.6033→0.7986，弥合 49.5%；
只换 part1b 在环出图 46.73→43.56，**有构图但无猫**）

> ⚠️ **补写说明（约束 4 的违反，如实记录）**：part1a / part2 的重量化我在
> **没有先写方案**的情况下就启动了（2026-08-15 22:24 / 2026-08-16 00:31）。
> 这违反约束 4「每个验证实验必须先规划后执行」。
> 本文件在**任何三段结果产生之前**补齐，判据与决策树均为事前定稿；
> 但"先规划后执行"这一条本轮**确实没做到**，不掩饰。

## 0. 假设

**H-row3**：per-row 对 part1b 的收益（E_all 19.24%→7.87%）在 part1a / part2 上同样成立；
三段级联后噪声预测误差从 47.07% 降到"好图阈值带"内，设备生成可辨识的图像。

**已知的定量边界（①实测）**：噪声预测相对 L2 **15.988% ⇒ 成图完好**；
**47.07% ⇒ 橙色色块**。真实阈值在 16%~47% 之间，**未测定**。

**一条明确的悲观理由（①事实，必须写在前面）**：
part2 的注意力 MatMul **两个输入都是激活、没有静态权重**，
`--use_per_row_quantization` 对它们**完全不起作用**。
part2 实测 axis-quant 张量 139 个、part1a 109 个、part1b 66 个，
但"有多少 MAC 落在被改善的算子上"**未测**。⇒ **part2 的收益很可能小于 part1b。**

## 1. 单变量

设备 `/data/local/tmp/htpcmp/` 的 `part1a.bin` / `part1b.bin` / `part2.bin`
三份**全部**换成 per-row 版；prompt、seed 42、分词、FP32 文本编码、调度器、VAE
**逐字节不变**；脚本沿用 `scripts/htp_inloop_pipeline.py`。
原件必须先备份（`*_ORIG.bin`），实验后还原。

## 2. 门禁

- G1：三份 `.bin` 的宿主/设备 md5 逐一核对一致
- G2：三份量化 DLC 均已确认 per-axis 生效（part1a **109**、part1b **66**、part2 **139** 个
  axis-quant 张量，全部 `axis: 0`）—— **已完成**
- G3：`htp_inloop_pipeline.py` 输出的 PNG 必须来自本次运行（先删旧文件再跑，核对时间戳）
- G4：8 步全部执行完毕，日志里每步的 `latents_std` 为有限值（非 nan/inf）

## 3. 判据（事前定稿）

记新图与 `zimage_fp32_full_pipeline.png` 的平均 |像素差| 为 `d`（`compare_images.py`）。
**已实测对照点**：Test B（CPU 参考跑量化 transformer）**9.32 / 23.89 dB，清晰的猫**；
三段全基线 **46.73 / 12.06 dB，零结构**；只换 part1b **43.56 / 12.60 dB，有构图无猫**。

| 分支 | 条件 | 判定 |
|---|---|---|
| **A** | `d ≤ 20` **且**图中能辨认出猫 | **顶层问题解决**（或接近解决） |
| **B** | 20 < `d` < 40，**或**图中出现可辨识物体（哪怕不是猫） | **显著改善但未成图** |
| **C** | `d ≥ 40` **且**仍无法辨认任何物体 | **三段全量仍不足** |

⚠️ **约束 1**：必须自己打开 PNG 看；"有没有猫"由肉眼判定，不由 `d` 单独决定。
⚠️ **约束 2**：`d` 与肉眼冲突时以肉眼为准，但**两者都要报**，不得只报有利的那个。

## 4. 🔴 决策树（**在结果出来之前锁定，不等坏结果再想办法**）

### 分支 A（出猫）
1. 三段 per-row DLC 固化，更新 `QNN_CONVERSION_GUIDE` 为推荐配方
2. 用设备 app 真机跑一次（级别 3 验证），确认与在环一致
3. 测生图时间（per-row 只多 65 MB，**预期不影响**，但要实测确认）

### 分支 B（有物体、无猫）——**当前我认为最可能**
**下一步锁定为 #53：激活 percentile 校准**，理由不是"没别的可试"，而是三条实测：
- #52：HTP 误差**随量化步长单调变化**（权重侧已验证两次，方向相反、结论一致）
- 15.12：`unified` 的 16-bit 量程里**主体只用到约 10 bit**，
  `--act_quantizer_calibration percentile`（p99.99）可收紧 scale **3.6 倍**
- `EXP_PLAN_CH85_CAUSAL`：离群点占 64% 能量却只造成 **23%** 下游损害，
  主体占 36% 能量造成 **99%** ⇒ **钳掉离群、换来主体分辨率细 3.6 倍，是划算的交易**
  （这正是 #9 当初关闭时没有考虑的角度，见 #53）

执行方式：**先只做 part1b 单段**（54 min 量化 + 14 min 建图 + 2 min 设备），
判据沿用同一套口径，比较对象是 **per-row 的 0.7986**：
`Δ ≥ +0.05` 才推进三段；`|Δ| < 0.005` 则 #53 否定，转分支 C 的路线。

### 分支 C（仍无结构）
说明 part1b 的张量改善**没有传导到成图**，继续在量化配置上试错的期望收益很低。
**下一步锁定为 #30：单 MatMul 探针**（台账里成本最低的一条，且从未做过）：
建一个只含 1 个 MatMul 的极简模型 + 受控输入，在 HTP 上跑，
**直接测出 HTP 内部的有效位宽与舍入策略**。
- 若测出存在硬性精度地板（如内部截断到 ~A12）⇒ 量化配置这条路到头了，
  转 **#32（SDK 2.28 vs 2.48 A/B）**——local-dream 用 2.28 在同款设备上跑通过 SD1.5/SDXL
- 若没有地板 ⇒ 回到 #53 / 选择性 FP16（#48 `--keep_weights_quantized`，wFxp_actFP）

### 三个分支共同的兜底
无论 A/B/C，**都要把"per-row 是有效手段"这条写进 `QNN_CONVERSION_GUIDE`**（已完成，第十一节），
因为它对下一个模型（Flux.2 Klein）是无条件有用的。

## 5. 预算

| 步骤 | 预算 | 状态 |
|---|---|---|
| part1a 量化 | 实测 **126 min** | ✅ 完成 |
| part2 量化 | 实测 **445 min**（含我重复启动导致的抖动，正常应 ~150 min） | ✅ 完成 |
| part1a 建图 | ~20 min | 🔄 进行中 |
| part2 建图 | ~25 min | ⏳ |
| 推三份 `.bin` + 在环出图 | ~30 min | ⏳ |

## 6. 执行纪律（本轮已犯错，固化为检查项）

启动任何长任务前**逐条核**：
1. `tasklist | grep -c <进程名>` **必须为 0**，启动后**必须为 1**——防重复启动
2. 每次启动用**独立日志路径**，禁止两个进程写同一个 `>` 重定向文件
3. **禁止用"日志为空 + 此刻无进程"推断后台任务已死**——
   本轮就是这样误判，导致 part2 被启动两次、机器换页一夜、多耗 5 小时
4. 重任务**一次只跑一个**；启动前确认空闲内存 ≥ 该任务预期峰值
5. 不再使用"等待后自动拉起下一步"的链式脚本——**每一步人工确认产物存在再启动下一步**
