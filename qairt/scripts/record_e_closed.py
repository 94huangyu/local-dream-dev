# -*- coding: utf-8 -*-
"""E 的最终判决：关闭。更新 #149/#150/#151 与 §6 的 E 行，并入指南 §41。"""
import io
import os
import shutil

ROOT = os.path.join("D:", os.sep, "LocalDreamZImage")
MAIN = os.path.join(ROOT, "MAINLINE.md")
GUIDE = os.path.join(ROOT, "docs", "QNN_CONVERSION_GUIDE.md")

ROW152 = (
    u"| 152 | \U0001F534\U0001F534 **E 最终判决：SmoothQuant 与「重标定 encoding」双双关闭；"
    u"根因是我的实现 bug + 真实的方向性失败，两者已分开** "
    u"| ✅ **已关闭·段级单变量实测（2026-08-31）** "
    u"| \U0001F534 **先说 bug（约束 R1：执行错 ≠ 方向错）**："
    u"#151 里 S5 出的**纯噪声图（9.99 dB）是我的实现 bug**，不是 SmoothQuant 的性质。<br>"
    u"　①**根因（靠新加的一致性断言当场抓出）**：建图的权重 `Mul(s)` 有守卫 "
    u"`sv.size == dims[0]`，**只对按 `[K,M]` 存放的权重生效**；"
    u"而 `adaLN_modulation` 那类是转置存放的 `[M,K]`（如 `[15360,256]`）⇒ 守卫为假 ⇒ "
    u"**权重不缩放、激活仍被 `Div(s)`** ⇒ 逐通道差一个 s 因子 ⇒ **静默毁段**。<br>"
    u"　①**四段全部不平衡**（part1a 4 个权重 / part1b 7 / part2a 8 / part2b 1）。"
    u"\U0001F534 **part1b 之所以在单段测试里正常，是因为它的 `adaln_input` 恰是【图输入】**"
    u"（激活循环 `if name in gin: continue` 跳过 Div）⇒ **两边都没做、凑巧平衡**。"
    u"⇒ ②**我唯一单独验证过的那一段，恰好是唯一因巧合而正确的段** —— "
    u"抽样验证选错对象时，验证本身会给出虚假的安全感。<br>"
    u"　\U0001F7E2 **修法已机制化**：建图后断言「凡被 `Div(s)` 的激活，其每个权重都拿到 `Mul(s)`」，"
    u"不平衡则**建图失败**。这把「静默毁段」变成了「硬失败」。<br>"
    u"\U0001F534 **修好之后，方向仍然是错的（这才是真结论）**：<br>"
    u"　①**段级单变量实测**（同输入、同 FP16 集合、断言配平、唯一变量是 alpha）：<br>"
    u"　　`add_138` 2.67% → **α=0: 2.70%** → **α=0.5: 17.77%**<br>"
    u"　　`unified` 3.82% → **3.05%** → 3.59%<br>"
    u"　　`add_92`  1.73% → **1.76%** → **16.67%**<br>"
    u"　　`latents` 3.60% → **3.56%** → **20.54%**<br>"
    u"　⇒ **α=0（只重标定）四段全部 ≈ 基线；全部恶化都来自【平滑本身】**，"
    u"三段差 **5~10 倍**。<br>"
    u"　②**机制（推论，非实测）**：`s = ax^0.5/aw^0.5` 让激活变均匀（激活量化变好），"
    u"但**权重乘 s 后逐输出通道要覆盖更宽的输入通道范围 ⇒ 权重量化变粗**。"
    u"而 #52 实测 HTP 对权重量化步长敏感 **37.6 倍**、#3.3 实测「权重对称量化更差、"
    u"与官方通用建议相反」⇒ **本模型输不起权重侧的代价**。<br>"
    u"\U0001F7E2 **「重标定 encoding」也判负**：段级 ≈ 基线，端到端 **−0.47 dB**（#151，图正常），"
    u"该臂实现里**没有**上述 bug ⇒ 结论可靠。<br>"
    u"\U0001F534 **最值钱的方法论产物**：**G0 门（MatMul 输出误差）过了，方向却是错的。**"
    u"G0 报 sum E² 降 **96%**，段级实测**差 5~10 倍** ⇒ "
    u"②**MatMul 层输出误差不能预测段级输出误差** —— 这是 #121（L1 不能预测 L3）"
    u"**在更低一层的重演**。已入指南 §41。<br>"
    u"⚠️ ③**未测**：修复后的**端到端 L3**没跑（段级 5~10 倍恶化方向一致且幅度大，"
    u"不值得再花 4 小时确认「更差」）。若将来要翻案，必须补这一测 |"
)

GUIDE_ADD = u"""

---

## 四十一、🔴 **代理指标的链条又断一环：MatMul 输出误差不能预测段级误差**（2026-08-31 实测）

本项目已经知道 **L1（step-0 噪声预测）不能换算成 L3（成图 PSNR）**（#121）。
这次发现更低一层同样断裂。

### 41.1 事故形态

评估 SmoothQuant 时设了一道便宜的代价门：对真实的 X 与 W 直接量 **MatMul 输出误差**。
结果非常漂亮：59 个 MatMul 的 **sum E² 降 96.0%**，误差能量集中的几个改善 65~93%。
判据（事前锁定 ≥20%）🟢 通过。

然后在**段级**（同一段的图输出）上单变量实测：

| 段输出 | 基线 | 只重标定 | **+SmoothQuant** |
|---|---|---|---|
| `add_138` | 2.67% | 2.70% | **17.77%** |
| `unified` | 3.82% | 3.05% | 3.59% |
| `add_92` | 1.73% | 1.76% | **16.67%** |
| `latents` | 3.60% | 3.56% | **20.54%** |

⇒ ②**三段差 5~10 倍。代理指标说好 96%，实际差一个数量级。**

### 41.2 为什么会断

MatMul 层的度量只看**那一个算子的输出**，看不见两件事：

1. **权重变粗的代价会传给下游所有依赖该权重的路径**，不止那一个 MatMul 的输出；
2. 段内还有归一化与残差，**局部改善不一定沿链存活**（本项目 #97：RmsNorm 有固有放大）。

### 41.3 规则

🔴 **任何「算子级 / 张量级」的代价门，只能用来【否决】，不能用来【放行】。**
过了门只说明「值得继续花钱测下一层」，**不构成方向成立的证据**。
必须逐层往上验，直到那个被标定过、与产品目标直接挂钩的量（本项目是 L3 成图 PSNR）。

⊕ 本项目已知的断裂点，逐层记下来：
表示误差 ↛ HTP 误差（#52，37.6 倍）；**MatMul 输出误差 ↛ 段级误差（本节，5~10 倍）**；
L1 ↛ L3（#121，判错 2/15 对）；宿主模拟 ↛ 设备（#123，比值 1.826 离群）。
"""


def main():
    s = io.open(MAIN, encoding="utf-8").read()
    L = s.split("\n")
    i = [k for k, l in enumerate(L) if l.startswith(u"| 151 | ")]
    assert len(i) == 1 and u"| 152 | " not in s
    shutil.copyfile(MAIN, os.path.join(ROOT, "logs", "MAINLINE.bak_152.md"))
    L.insert(i[0], ROW152)
    j = [k for k, l in enumerate(L) if l.startswith(u"| **E** | #20 ")]
    assert len(j) == 1, "定位 §6 E 行失败"
    L[j[0]] = (u"| ~~**E**~~ | ✅ **已关闭（2026-08-31）**：SmoothQuant 段级实测"
               u"**差 5~10 倍**；「重标定 encoding」端到端 **−0.47 dB** | 已完成 "
               u"| 两条都判负，详见 #152。⚠️ 关闭的是**这两种具体手段**，"
               u"不是「一致性方向」整体 —— 顶层目标仍未推进 |")
    io.open(MAIN, "w", encoding="utf-8", newline="").write("\n".join(L))
    print("OK #152 已登记，§6 的 E 已关闭")

    g = io.open(GUIDE, encoding="utf-8").read()
    assert u"## 四十一、" not in g and u"## 四十、" in g
    shutil.copyfile(GUIDE, os.path.join(ROOT, "logs", "GUIDE.bak_before41.md"))
    io.open(GUIDE, "w", encoding="utf-8", newline="").write(g.rstrip() + GUIDE_ADD)
    print("OK 指南 §41 已新增")


if __name__ == "__main__":
    main()
